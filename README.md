Simple Notifier For Mac OS X
============================

Install
-------

Install to `/usr/local/bin/notify` or anywhere else in your `PATH`.

Usage
-----

    $ sleep 5 && echo 'some long command' && notify "done"
